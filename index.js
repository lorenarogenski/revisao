const prompt = require("prompt-sync")();

while(true) {
    console.log(`
    Menu: 
    1. Cadastrar Produto.
    2. Listar Produtos.
    3. Atualizar Produtos.
    4. Remover Produtos.
    5. Sair.`)
}

let escolha = prompt("Escolha uma opção: ");

switch (escolha) {
    case "1":
        
        
        break;

    default:
        break;
}